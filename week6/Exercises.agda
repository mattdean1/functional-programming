{-
Implement some standard library functions for Vec:
  head / tail
  append
  reverse
  map
  fold

Consider several implementations of "safe indexed access" in order to examine the various trade-offs you need to make.

Implement an Agda data-type for "singleton" natural numbers, where m : Singleton n if and only if m = n.
Consider addition and its properties on this type. Consider an implementation of safe indexed access which uses this type. 
-
